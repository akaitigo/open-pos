version: v2
modules:
  - path: .
    name: buf.build/openpos/api
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
  except:
    - FILE_SAME_JAVA_MULTIPLE_FILES
