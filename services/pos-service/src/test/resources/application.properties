# Test profile configuration

# Database: H2 in-memory
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS pos_schema
quarkus.datasource.username=sa
quarkus.datasource.password=
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.database.default-schema=pos_schema

# Flyway: disable in test (Hibernate manages schema)
quarkus.flyway.migrate-at-start=false

# Redis: disable in test
quarkus.redis.devservices.enabled=false

# RabbitMQ: disable in test
quarkus.rabbitmq.devservices.enabled=false
mp.messaging.outgoing.outgoing-events.connector=smallrye-rabbitmq
mp.messaging.outgoing.outgoing-events.exchange.name=openpos.events.test
mp.messaging.outgoing.outgoing-events.exchange.type=topic

# gRPC Client: product-service (not used in unit tests, mocked)
quarkus.grpc.clients.product-service.host=localhost
quarkus.grpc.clients.product-service.port=9999

# gRPC: use random test port
quarkus.grpc.server.port=0
quarkus.grpc.server.test-port=0

# HTTP: enable with random port for health check tests
quarkus.http.port=8080
quarkus.http.test-port=0

# Datasource health check: disable (no real DB in test)
quarkus.datasource.health.enabled=false

# OpenTelemetry: disable in test
quarkus.otel.enabled=false

# Scheduler: disable automatic execution in test
quarkus.scheduler.enabled=false
